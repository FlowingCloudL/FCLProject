<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fp.mall.sale.mapper.FlashsaleSessionMapper">

    <select id="listByFlashsaleId" parameterType="Long" resultType="FlashSaleSessionEntity">
        select * from `sms_flashsale_session` where `flashsale_id` = #{flashsaleId}
    </select>

</mapper>